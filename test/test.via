import std::io;
import std::math;

if false {
    io::printn("this should never run");
}

while true {
    if true {
        io::printn("done");
    }
}

io::printn("sine calculator");
var input = io::input("enter a number: ");
var result = math::sin(input as float)
io::printn(result as string);
