import std::io;
import std::math;

var printn = io::printn;
var input = io::input;

while true {
    printn("sine calculator");
    var input = input("enter a number: ");
    var result = math::sin(input as float)
    printn(result as string);
}
