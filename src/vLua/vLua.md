# vLua

vLua is a custom Lua compiler that is embeded into the [via programming language](https://github.com/XnLogicaL/via-lang)
used for compiling Lua code into via bytecode.
This is NOT a standalone application as it requires via runtime to be executed, and it's only inteded use is embeding Lua code into via code.