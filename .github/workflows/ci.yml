name: via-lang CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build_and_test:
    runs-on: ubuntu-latest

    steps:
      # Checkout code
      - name: Checkout repository
        uses: actions/checkout@v2

      # Set up Python
      - name: Set up Python 3.x
        uses: actions/setup-python@v2
        with:
          python-version: '3.12'

      - name: Build interpreter
        run: |
          mkdir build
          cmake -S . -B build -D DEBUG_MODE=OFF
          cmake --build build

      # Run Python tests
      - name: Run tests
        run: |
          python scripts/run_tests.py
